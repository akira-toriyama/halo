name: build

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  # Cancel in-flight builds on the same PR / branch when a new push
  # arrives — only the latest commit matters for the required check.
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    # Bounded so a hung `swift build` can't burn 6 h of free macOS minutes.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      # Shared Swift build (setup-xcode + SPM cache + build). halo has no
      # test target yet (single-threaded agent over private-SkyLight
      # callbacks; nothing pure to unit-test) → run-tests: "false".
      - uses: akira-toriyama/.github/actions/swift-build@main
        with:
          run-tests: "false"

      - name: Assemble & ad-hoc sign app bundle
        env:
          CODESIGN_ID: "-"
        run: ./package.sh
